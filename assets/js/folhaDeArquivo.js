const listaDiretorios = document.getElementById('arquivo-container')
const abaDiretorios = document.querySelector('.aba-paginas')
const folhaArquivoContainer = document.getElementById('editor-content')

    /* a constelação será montada com as pastas que forem 
    indentificadas como clases na lista de diretorio */

    // 1 - adicionar arquivos na lista do explorador
    // 2 - ao criar um arquivo .class 
        // 1 - cria um objeto 'star'
            // - classe main ou master sendo o 'Sol'
            // - 'planetas' sendo classes diversas
            // - atributos e metodos sendo suas 'luas'
        // 2 - referenciar e ligar as classes(astros) pelas classes que o chamam
    // 3 - se clicado no arquivo abre como folha de Edição e na aba do navegador e amplia objeto 'Astro' 

class Page {
    

    constructor(name, content ) {

        // pagina
        
        // let pagina = document.createElement('textarea')
        // pagina.setAttribute('name', name)
        // pagina.setAttribute('className, name')

        // star

    }
    escolhePaginaAba(params) {
        // pagina
        // aba
        // star    
    }
    fechaPaginaAba() {
        // pagina
        // aba
    }

}
// dectAll = docs

// for(let i in decAll) {
//     new Page(i.name, i.content)
// }

// fila de abas